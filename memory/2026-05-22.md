# 2026-05-22

## Session: Black Ink Signal MVP v1 complete

- Bootstrapped workspace identity: **Inkscout** (signal-hunting studio ops familiar)
- User: **Al**
- Project: **Black Ink Signal** — desktop lead intelligence system for tattoo studios

### What was built (all 7 milestones)
1. **SQLite schema** — leads, lead_events, source_runs tables via SQLAlchemy
2. **Lead scoring engine** — 1-100 score from intent/geo/recency/project/urgency/engagement/coverup
3. **Reddit connector** — public JSON fetcher (needs OAuth or residential IP for prod)
4. **FastAPI backend** — 12 endpoints: health, leads CRUD, status/bookmark/notes, enrich, search, CSV export, stats
5. **Desktop live feed** — React+Vite dark operator UI: lead cards, detail drawer, status buttons, bookmarks, 30s auto-refresh
6. **Filters/search** — sidebar score/status filters, text search, bookmarked-only toggle
7. **AI classification** — rule-based classifier (always-on) + LLM classifier (optional), enrichment pipeline

### Verified working
- FastAPI boots on :8787, all endpoints respond correctly
- Scoring: Edmonton sleeve = 70 (strong), meme = 1 (filtered), coverup Alberta = 36
- Classifier: coverup → frustrated/immediate/60% booking, memorial → serious/70% booking, meme → 5% booking
- Batch enrichment: 14/14 leads enriched in single call
- CSV export with filters works
- Search across title/body/keyword/intent works
- Frontend builds clean (vite 5.4.21, 1.13s)
- 15 realistic seed leads in DB

### Known limitations
- Reddit blocks public JSON from cloud IPs (403) — needs OAuth app or residential IP
- LLM enrichment requires BIS_LLM_API_KEY env var
- Desktop Electron shell not yet wired (just Vite dev/build)
- No desktop notifications yet

### Commits
- d0355f1 Add Black Ink Signal repo skeleton
- ccd99b0 Add SQLite schema, scoring engine, Reddit connector, and FastAPI server
- c4f1d42 Fix Python path imports for uvicorn compatibility
- 1dfbe46 Add live desktop feed with lead cards, drawer, filters, search, and status actions
- 9591464 Add AI classification layer, enrichment pipeline, search, and CSV export
