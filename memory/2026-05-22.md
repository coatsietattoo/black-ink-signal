# 2026-05-22

## Session: Black Ink Signal build stage 2 complete

### Milestones delivered

**1. Scoring v2 (tuned)**
- Complete rewrite of scoring engine
- 15/15 test cases pass against target bands
- Explicit local buying intent: 85-100 (hot)
- Coverup/sleeve/memorial + local: 80-95 (hot)
- Style search + local: 75-90 (strong-hot)
- Tattoo intent without location: 40-60 (watchlist)
- Memes/no intent: 1-4 (low)
- Fixed substring bug ("rip" matching inside "script")
- Geo scaling: buying intent gets full geo bonus, moderate/style gets 80%

**2. Background scheduler**
- APScheduler with configurable intervals (default: Reddit 5m, enrichment 2m)
- Runs immediately on start then on interval
- Auto-selects OAuth vs public connector
- Graceful shutdown on SIGINT/SIGTERM
- Integrates notification dispatch after ingestion

**3. Desktop notifications**
- System notifications (Linux notify-send, macOS osascript, Windows toast)
- In-app notification bell with red pulse animation
- Notification panel dropdown with hot lead list
- Cooldown to prevent re-alerting same lead
- API endpoints: GET /notifications, POST /notifications/trigger
- Tested: 7 hot lead notifications fired correctly

**4. Reddit OAuth**
- Full OAuth2 "script" app connector
- Auto-selects: OAuth if credentials set, public JSON fallback
- Token refresh, rate limit handling, 429 retry
- .env.example with all config vars
- Separate oauth_connector.py, isolated from public connector

**5. Electron packaging**
- electron/main.js with window, tray, minimize-to-tray
- Dark background (#09090b) matching the UI
- Native notification support (click to focus window)
- electron-builder config for mac/win/linux packaging
- npm scripts: electron:dev, electron:build, start

### Commits (this session)
- ff665fe Scoring v2: rewrite scoring engine — 15/15 test cases pass
- fe5cab9 Background scheduler (APScheduler, configurable, graceful shutdown)
- 90991b7 Desktop notifications (system + in-app bell, cooldown, API)
- e5cf829 Reddit OAuth connector with auto-selection
- a6b6cfd Electron shell (tray, minimize-to-tray, native notifications)
- ed8d481 README with architecture, quick start, configuration

### What runs
- FastAPI boots on :8787, 14 endpoints respond
- Scoring v2 passes all 15 test scenarios
- Classifier enriches all leads (rule-based, LLM optional)
- Scheduler runs both jobs on configurable intervals
- 7 hot lead notifications fire correctly
- Frontend builds clean (1.14s)
- CSV export works with filters
- Search works across all fields

### Build stage 3 — local readiness + real data

**1. LOCAL_SETUP.md** — complete Mac setup guide
- Python + Node prereqs
- venv setup
- Reddit OAuth walkthrough with screenshots-level detail
- 3-terminal quick launch instructions
- Troubleshooting section

**2. .env.example expanded**
- All config vars documented
- API host/port, DB path, subreddits, target city/region
- Clean sections for Reddit OAuth, scheduler, notifications, LLM

**3. Source health panel**
- GET /sources/health — Reddit status, OAuth, last fetch, 24h stats, enrichment, scheduler
- Admin view in desktop UI with live health cards (green/yellow dots)
- Auto-refreshes every 30s

**4. Admin action buttons**
- Fetch Reddit Now
- Enrich Leads
- Rescore All
- Seed Demo Data
- Export CSV
- Clear Demo Data
- Action log in admin panel
- POST /admin/fetch-reddit, /admin/enrich, /admin/seed, /admin/rescore
- DELETE /admin/clear-demo, /admin/clear-all

**5. Expanded lead review statuses**
- new → reviewing → contacted → booked → follow_up → saved → bad_match → dismissed
- Color-coded in UI: booked=green, follow_up=purple, bad_match=red

**6. Daily summary**
- GET /stats/daily endpoint
- Shows: hot today, strong today, status breakdown, top keywords, top sources, top intents
- Rendered in admin panel

### Commits (this session)
- 280a8ff LOCAL_SETUP.md + .env.example
- 3b5e84b Source health, admin actions, expanded statuses, daily summary
- dd7510e V1 hardening: onboarding, error-safe, dedup, scripts, booked value, backup

### Build stage 4 — V1 hardening (dd7510e)

**1. Onboarding screen**
- Shows when: no Reddit OAuth AND no leads in DB
- Checklist: API server, Reddit OAuth (with setup instructions), Lead data (seed button), Scheduler
- Can seed demo data directly from onboarding
- "Continue anyway" option if you just want to explore

**2. Error-safe mode**
- API errors caught gracefully — red banner with error message + retry button
- App stays open and functional even if backend is unreachable
- No more broken white screen on connection failure

**3. Duplicate protection report**
- GET /admin/dedup-report explains dedup strategy
- Method: dedupe_key = source:item_id (e.g. reddit:t3_abc123)
- Shows: total leads, unique source IDs, recent run stats (seen/added/skipped/dedup rate)

**4. Contact script generator**
- GET /leads/{id}/scripts generates 3 human-written reply options
- Tones: soft/helpful, direct consultation, funny/casual
- Context-aware: different scripts for memorial, coverup, first tattoo, sleeve, artist search
- Copy-to-clipboard buttons in the drawer UI

**5. Booked value**
- New column: leads.booked_value (auto-migrated on startup)
- PATCH /leads/{id}/booked-value — accepts: small, half_day, full_day, sleeve_project, custom:NNN
- Shows in drawer when status = booked
- GET /stats/revenue — revenue summary from booked leads
- Value map: small=$150, half_day=$500, full_day=$1000, sleeve_project=$3000

**6. Backup/export**
- POST /admin/backup — timestamped SQLite backup, keeps last 10
- GET /admin/backup/download — downloads current DB file
- Buttons in admin panel

### Build stage 5 — operational polish + signal quality

**1. Live feed atmosphere**
- Incoming lead slide-in animation (0.4s ease)
- Hot lead soft pulse glow (3s infinite cycle)
- Ambient notification sound via Web Audio API (880Hz sine ping)
- Sound toggle in sidebar
- Activity ticker showing latest incoming leads
- Compact mode for side-monitor use (smaller cards, no body preview, no action buttons)
- "just now" / "Xm ago" / "Xh ago" / "Xd ago" timestamps

**2. Smart lead aging**
- Visual opacity decay: 1.0 (fresh) → 0.95 (6h) → 0.85 (24h) → 0.7 (48h) → 0.55 (72h) → 0.4 (older)
- Leads marked contacted/saved/booked/follow_up don't fade
- Bookmarked leads don't fade
- Subtle but immediate visual signal of what needs attention

**3. Score confidence explanation**
- Every lead now carries full `score_breakdown` object in API response
- ScoreExplainer component in drawer shows each factor:
  - 🎯 +48 Intent signal
  - 📍 +25 Location match
  - 🖼 +12 Project size
  - ⚡ +10 Urgency
  - 🗨 +5 Engagement
  - 🕐 +6 Recency
  - ⚠️ -5 Noise penalty
- Live-computed, always matches current engine version

**4. Semantic search**
- TF-IDF index with domain-boosted vocabulary (50+ tattoo terms)
- Cosine similarity ranking
- GET /search/semantic?q=... — fuzzy concept search
- GET /leads/{id}/similar — find related leads
- POST /admin/rebuild-index — rebuild after bulk changes
- Handles 10k+ docs, builds in 0.3s, searches in 43ms
- Detects intent even without exact keyword matches

**5. Trend engine**
- GET /stats/trends?days=7
- Rising keywords (first-half vs second-half comparison)
- Intent distribution
- Busiest sources (subreddits)
- Peak hour (UTC) + hourly distribution
- Peak day of week + daily distribution
- Geo clusters
- Score band distribution

**6. Session persistence (localStorage)**
- Filters survive reload
- Compact mode preference persists
- Sound toggle persists
- Active view (feed/admin) persists
- Selected lead restored on reload

**7. Performance pass**
- Stress tested: 10,015 leads
- Scoring: 19,803 leads/sec
- DB insert: 12,512 leads/sec
- Hot query: 2.9ms
- Text search: 2.3ms
- Semantic search: 42.7ms
- Index build (10k docs): 0.32s
- All queries < 50ms at scale

### Commits
- 90ca89b Live feed atmosphere, lead aging, score explainer, session persistence, compact mode
- c4932c3 Semantic search, trend engine, performance pass
- df6f745 Final V1 architecture docs + deployment checklist

### Final V1 totals
- **31 API endpoints**
- **39 frontend modules**
- **20 commits on master**
- **Frontend**: 20KB CSS (4KB gzip), 410KB JS (111KB gzip)
- **Build time**: 1.18s
- **Performance**: all queries < 50ms at 10k leads
